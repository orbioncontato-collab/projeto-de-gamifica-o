/**
 * Helpers de teste promovidos para `@/test/render` (handoffs WP5/WP7 — integração onda 3).
 * Reexport mantido para os imports existentes das features.
 */
export { makeBootstrap, makeMe, renderInRouter, TEST_SETTINGS } from '@/test/render'
